---
title: openship.json
description: The declarative deploy config for Openship — like vercel.json or railway.toml. Declare framework, build, runtime, env, domains, routes, resources, services and monorepo layout, and Openship deploys the same way every time.
---

import { TypeTable } from 'fumadocs-ui/components/type-table';

`openship.json` is a repo-root file that tells Openship how to build, run, route, and scale a
project. It mirrors what you'd otherwise set in the [deploy wizard](/docs/guides/deploy-from-github),
so a repo becomes "ready to ship on Openship" and [auto-deploy on push](/docs/guides/auto-deploy)
and `openship deploy` become deterministic — no wizard needed.

<Callout title="Authoritative overlay">
Openship **auto-detects** your stack first, then applies every field present in `openship.json`
on top. Fields you omit keep their detected value. So a good `openship.json` is **small** —
declare only what you want to override or pin, not the whole detected config.
</Callout>

Add the `$schema` line for editor autocomplete and inline validation:

```json
{
  "$schema": "https://openship.io/openship.schema.json",
  "framework": "nextjs",
  "port": 3000
}
```

Validate it any time with the CLI:

```bash
openship config init       # scaffold a starter openship.json
openship config validate   # check it against the schema
```

It's **JSON, not JSONC** — no comments, no trailing commas.

## Build

<TypeTable
  type={{
    framework: { type: 'string (enum)', description: 'Stack slug — nextjs, vite, django, go, docker, …. Overrides detection.' },
    packageManager: { type: 'string (enum)', description: 'npm, yarn, pnpm, bun, go, cargo, pip, poetry, pipenv, uv, bundler, composer, maven, gradle, dotnet, mix.' },
    rootDirectory: { type: 'string', description: 'App directory relative to the repo root (e.g. "./", "apps/web").' },
    installCommand: { type: 'string', description: 'Command that installs dependencies.' },
    buildCommand: { type: 'string', description: 'Command that builds the app.' },
    startCommand: { type: 'string', description: 'Command that starts the server in production.' },
    outputDirectory: { type: 'string', description: 'Directory the build writes to (dist, .next, build, out).' },
    buildImage: { type: 'string', description: 'Docker image used for the build (e.g. "node:22").' },
    productionPaths: { type: 'string[]', description: 'Paths shipped to the runtime as the production artifact.' },
  }}
/>

## Runtime

<TypeTable
  type={{
    runtime: { type: '"bare" | "docker"', description: 'Runtime isolation for a single app. Services/docker projects are always "docker".' },
    productionMode: { type: '"host" | "static" | "standalone"', description: '"static" ⇒ served as files, no server.' },
    port: { type: 'integer', description: 'Port the server listens on (1–65535).' },
  }}
/>

## Environment variables

`env` is an object. A value is a plain string, or `{ "value", "secret" }` to mark a secret
(encrypted at rest — never returned to the client afterward).

```json
{
  "env": {
    "NEXT_PUBLIC_URL": "https://app.acme.com",
    "DATABASE_URL": { "value": "postgres://…", "secret": true }
  }
}
```

## Domains

`domains` is an array of hostnames or objects. A bare label (`"myapp"`) is a free
`*.opsh.io`-style subdomain; a dotted hostname (`"app.acme.com"`) is a [custom domain](/docs/guides/custom-domains).

```json
{ "domains": ["app.acme.com", { "domain": "api.acme.com", "port": 8080, "type": "custom" }] }
```

<TypeTable
  type={{
    domain: { type: 'string', description: 'Hostname. Bare label = free subdomain; dotted = custom.' },
    port: { type: 'integer', description: 'Which port this hostname routes to.' },
    targetPath: { type: 'string', description: 'Path prefix on the target (default "/").' },
    type: { type: '"free" | "custom"', description: 'Overrides the free/custom inference.' },
  }}
/>

## Routes

`routes` reproduces `vercel.json`-style routing, compiled to the reverse proxy at deploy.

<TypeTable
  type={{
    rewrites: { type: '{ source, destination }[]', description: 'Internal rewrites (e.g. SPA fallback).' },
    redirects: { type: '{ source, destination, permanent?, statusCode? }[]', description: '3xx redirects.' },
    headers: { type: '{ source, headers: { key, value }[] }[]', description: 'Response headers per path.' },
    cleanUrls: { type: 'boolean', description: 'Strip .html extensions.' },
    trailingSlash: { type: 'boolean', description: 'Enforce or remove trailing slashes.' },
  }}
/>

## Resources

Cloud sizing — a named tier, **or** explicit values (which become the `custom` tier). Ignored on
self-hosted instances. See [Sleep mode & resources](/docs/guides/sleep-mode-resources).

<TypeTable
  type={{
    tier: { type: '"micro" | "low" | "medium" | "high"', description: 'Named sizing tier.' },
    cpuCores: { type: 'number', description: 'Fractional vCPU cores (0.25–4).' },
    memoryMb: { type: 'integer', description: 'RAM in MB (128–8192).' },
    diskMb: { type: 'integer', description: 'Disk in MB (64–204800).' },
  }}
/>

## Services (compose)

Declaring `services` makes the project a multi-service (Docker) project — see
[Compose / multi-service](/docs/guides/compose-multi-service). Each entry requires a `name`.

```json
{
  "services": [
    { "name": "web", "build": ".", "ports": ["3000"], "exposed": true, "domain": "app.acme.com" },
    { "name": "db", "image": "postgres:17", "volumes": ["pgdata:/var/lib/postgresql/data"],
      "env": { "POSTGRES_PASSWORD": { "value": "…", "secret": true } }, "restart": "unless-stopped" }
  ]
}
```

<TypeTable
  type={{
    name: { type: 'string', description: 'Required. Service name.' },
    image: { type: 'string', description: 'Prebuilt image (e.g. postgres:17).' },
    build: { type: 'string', description: 'Build context path.' },
    dockerfile: { type: 'string', description: 'Dockerfile path.' },
    ports: { type: 'string[]', description: 'e.g. ["3000"], ["5432:5432"].' },
    volumes: { type: 'string[]', description: 'e.g. ["pgdata:/var/lib/postgresql/data"].' },
    dependsOn: { type: 'string[]', description: 'Other service names this one depends on.' },
    env: { type: 'env object', description: 'Same shape as top-level env.' },
    command: { type: 'string', description: 'Override the container command.' },
    restart: { type: '"no" | "always" | "on-failure" | "unless-stopped"', description: 'Restart policy.' },
    exposed: { type: 'boolean', description: 'Publicly routed.' },
    exposedPort: { type: 'string', description: 'Which container port is exposed.' },
    domain: { type: 'string', description: 'Public hostname for this service.' },
    healthcheck: { type: 'object', description: '{ test, interval, timeout, retries, startPeriod, disable }.' },
  }}
/>

## Monorepo

`monorepo` overrides Openship's detected sub-apps. Entries in `apps[]` are matched to detected
sub-apps by `rootDirectory` and override their build settings — they don't declare apps from
scratch (the detector finds the apps).

```json
{
  "monorepo": {
    "workspace": { "packageManager": "pnpm", "prepareCommand": "pnpm install && pnpm codegen" },
    "apps": [
      { "name": "web", "rootDirectory": "apps/web", "framework": "nextjs", "port": 3000 },
      { "name": "api", "rootDirectory": "apps/api", "framework": "hono", "port": 8080 }
    ]
  }
}
```

<TypeTable
  type={{
    'workspace.packageManager': { type: 'string', description: 'Root workspace package manager.' },
    'workspace.prepareCommand': { type: 'string', description: 'Runs once at the repo root before per-app builds.' },
    'apps[].name': { type: 'string', description: 'Required.' },
    'apps[].rootDirectory': { type: 'string', description: 'Required. Matches a detected sub-app.' },
    'apps[]…': { type: 'build fields', description: 'framework, packageManager, installCommand, buildCommand, startCommand, outputDirectory, buildImage, port.' },
  }}
/>

<Callout title="Not supported yet" type="warn">
`sleepMode`, monorepo `sharedPaths`, and per-app `domain`/`env`/`exposed` are accepted by the
validator but **not applied** — leave them out. Set sleep mode and per-app domains in the
dashboard for now.
</Callout>

## How it maps

| `openship.json` | Deploy wizard / API |
|---|---|
| `framework`, `packageManager`, `*Command`, `outputDirectory`, `buildImage`, `productionPaths`, `rootDirectory` | Build settings |
| `port`, `productionMode`, `runtime` | Runtime settings (`hasServer`, runtime isolation) |
| `env` | Environment variables (seeded as editable rows) |
| `domains` | Public endpoints / custom domains |
| `routes` | Reverse-proxy routing (`routingConfig`) |
| `resources` | Cloud resource tier / custom sizing |
| `services` | Compose services |
| `monorepo` | Monorepo sub-apps + workspace |

## See also

- [Guide: deploy with openship.json](/docs/guides/openship-json)
- [Deploy from GitHub](/docs/guides/deploy-from-github) · [Auto-deploy on push](/docs/guides/auto-deploy)
